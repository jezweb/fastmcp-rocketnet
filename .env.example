# Rocket.net API Credentials
# Option 1: Username/Password (most common)
ROCKETNET_USERNAME=your-email@example.com
ROCKETNET_PASSWORD=your-password

# Note: You can also pass username/password directly in tool calls
# for shared or multi-tenant scenarios

# Optional: API Base URL (defaults to https://api.rocket.net/v1)
# ROCKETNET_API_BASE=https://api.rocket.net/v1

# Optional: API Rate Limiting
# ROCKETNET_RATE_LIMIT_REQUESTS=100
# ROCKETNET_RATE_LIMIT_PERIOD=60

# Optional: Logging Level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO